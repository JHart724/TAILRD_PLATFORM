# Dependencies
node_modules/

# Keep root lockfile ignored, but allow backend lockfile
package-lock.json
!backend/package-lock.json

# Build output
build/
dist/
.tmp/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Coverage
coverage/

# Environment files
.env
.env.*

# Editor directories and files
.vscode/
.idea/
*.swp
.DS_Store
.codesandbox/
.claude/worktrees/
clean_gaps.js
dedup_gaps.js
fix_gaps.js
recalibrate.js
.claude/settings.local.json
.claude/*.local.json

# Terraform
**/.terraform/
terraform.tfvars
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
.gstack/

# Phase 2A temporary artifacts
.tmp-phase2a/
.tmp-phase3consolidation/

# Lambda build artifacts
infrastructure/lambdas/*/node_modules/
infrastructure/lambdas/*.zip
.tmp-day4/
.tmp-day5/

# Claude Code session artifacts (added 2026-05-03 cleanup)
.claude/scheduled_tasks.lock
.context/

# Audit pre-flight intermediate artifacts (legacy auditPreflight.ts).
# Canonical infrastructure under docs/audit/canonical/ is the source of truth.
backend/scripts/preflight-out/
docs/audit/preflight/

# Audit canonical intermediate output (regenerable via verifyDraft.ts;
# canonical .crosswalk.json files are the committed source of truth)
docs/audit/canonical/*.crosswalk.candidate.json

# Migration script summary artifacts (compliance evidence — operator archives
# to S3 per AUDIT-022 runbook §5.3; not committed to git)
backend/var/

# AUDIT-016 PR3 STEP 1.7 diagnostic artifact backlog (64 files; closed register-side 2026-05-18)
/audit-016-*.json
/audit-016-*.txt
/audit-016-*.md
/audit-016-*.b64
/audit-016-*.ts
/runtask-override-*.json
/pr-body-*.md
/done-*.json
/ct-events.json
/commit-msg-*.txt

# Phase 0A Phase 4 audit pre-existing draft artifacts (added 2026-05-19 methodology PR; sister-section to AUDIT-016 PR3 STEP 1.7 backlog above; leading-slash anchoring per §17.1 16th entry)
/docs/audit/V2_*_DAY11.md
/docs/audit/AGENT_DRIFT_REGISTRY_DRAFTS_*.md
# Clinical-code verification sweep run-output (transient; advisory triage report + resume checkpoint, regenerated by backend/scripts/sweepClinicalCodes.mjs). AUDIT-118 Work-Block 1 disposition 2026-06-14.
/docs/audit/sweeps/
